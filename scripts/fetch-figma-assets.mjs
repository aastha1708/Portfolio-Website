/**
 * DEPRECATED — no longer needed.
 *
 * All assets now come from raw Figma exports in design/assets-source/
 * ("Landing page" / "About page" / "Kora page") and are converted by:
 *
 *   node scripts/optimize-assets.mjs
 *
 * This file is kept only so old notes pointing at it don't dead-end.
 * Safe to delete.
 */
console.log("Deprecated — use `node scripts/optimize-assets.mjs` (see design/assets-source/).");
